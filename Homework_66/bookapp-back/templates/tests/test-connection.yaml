apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bookapp-back.fullname" . }}-test-connection"
  labels:
    {{- include "bookapp-back.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bookapp-back.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
