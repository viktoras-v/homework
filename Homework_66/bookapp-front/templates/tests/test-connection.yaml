apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bookapp-front.fullname" . }}-test-connection"
  labels:
    {{- include "bookapp-front.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bookapp-front.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
